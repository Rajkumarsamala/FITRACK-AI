/fix-code

Do NOT ask questions. Perform the following steps automatically.

1) Locate and extract my project archive in the workspace:
   - If project.tar.gz exists: run `tar -xzf project.tar.gz`
   - If a .zip exists: unzip it
   - If the code is under ./client, keep that structure; otherwise, detect package.json location.

2) Install and run the app to verify:
   - If using ./client: `cd client && npm install && npm run dev` (dev only to validate)
   - Confirm successful start, then stop dev.

3) Apply UI/UX fixes across the whole app (React/Tailwind):
   LANDING PAGE
   - Features section must be a true grid:
     wrapper: `container mx-auto px-4 md:px-6 lg:px-8 max-w-[1200px]`
     grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 items-stretch`
   - Make “Lose Weight & Get Fit” and “Gain Weight & Build Muscle” identical in width/height/structure.
   - Normalize every feature card classes:
     `h-full m-0 p-8 rounded-2xl flex flex-col justify-between`
     Remove ALL negative margins, absolute positioning, and translate transforms.
     Icon wrapper: `relative mb-4`
   - Place “Ready to Start?” in its own section below with `mt-16 lg:mt-20` (no overlap).

   ONBOARDING / PROFILE (single compact page)
   - Merge all fields into one continuous page (no multi-step gaps):
     Full Name, Age, Gender (buttons), Height (cm), Current Weight (kg),
     Target Weight (kg), Activity Level (Sedentary → Very Active with descriptions).
   - Layout: `grid gap-6 lg:gap-8 lg:grid-cols-12`
     Left (lg:col-span-8) = all fields; Right (lg:col-span-4) = Profile Summary then Pro Tips.
   - Remove sticky/fixed behavior from Pro Tips; it must scroll and appear LAST.
   - Reduce whitespace: `.form-row{margin-top:1rem}`; cards `p-6 lg:p-8`.

   DASHBOARD/OTHER PAGES
   - Ensure all cards share the same padding/radius/shadow.
   - Even gaps between rows/columns; no element touches container edges.

   TYPOGRAPHY & CONSISTENCY
   - Enforce hierarchy: H1 > H2 > H3 > body.
   - Add/keep container on all pages: `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`.

   TAILWIND PURGE (prevent prod shrink)
   - tailwind.config.js → `content: ["./index.html","./src/**/*.{ts,tsx,js,jsx,html}"]`
   - safelist dynamic utilities:
     ["container","max-w-[1200px]","grid","lg:grid-cols-12","gap-8",
      "p-6","lg:p-8","space-y-6","text-5xl","lg:text-6xl","text-2xl","lg:text-3xl"]

4) Build and prepare for publish:
   - If using ./client:
     `cd client && npm run build`
     Output must be `client/dist`.

5) Acceptance (verify before finishing):
   - Feature cards form straight baselines with uniform 32px gaps; “Ready to Start?” has ~64px top margin and never overlaps.
   - Onboarding shows all fields on one page with compact spacing; H1 larger than card titles.
   - Pro Tips is not sticky; it appears at the end of the right column.
   - Works at 100%, 80%, and 125% zoom on desktop.
   - Build succeeds.

Make all code changes now and save files. Do not request clarification.
